{
    "name": "app-status",
    "version": "1.0.0",
    "scripts": {
        "start": "cds-serve",
        "watch": "cds watch",
        "build": "cds build --production",
        "deploy": "cds deploy --to hana"
    },
    "dependencies": {
        "@sap/cds": "^9.4.4",
        "@sap/hana-client": "^2",
        "@sap/xsenv": "^4",
        "@sap/xssec": "^3",
        "axios": "^1",
        "express": "^4",
        "jsonwebtoken": "^9"
    },
    "engines": {
        "node": ">=18"
    },
    "cds": {
        "requires": {
            "auth": {
                "kind": "xsuaa"
            },
            "db": {
                "kind": "hana"
            }
        }
    }
}
